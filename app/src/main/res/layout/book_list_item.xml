<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/list_item_layout_margin"
    android:background="@android:drawable/dialog_holo_light_frame"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imageView"
            style="@style/iv_book_style"
            android:src="@drawable/no_image" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/textView_title"
                style="@style/tv_title_style"
                android:text="@string/default_title" />

            <TextView
                android:id="@+id/textView_author"
                style="@style/tv_author_style"
                android:text="@string/default_author" />

            <TextView
                android:id="@+id/textView_PDate"
                style="@style/tv_pDate_style"
                android:text="@string/default_published_date" />

        </LinearLayout>

    </LinearLayout>

    <TextView
        android:id="@+id/textView_description"
        style="@style/tv_description_style"
        android:text="@string/default_description" />
</LinearLayout>